# ALPHA ZERO FOR 'Cờ Gánh'
To start training a model:
```bash
python main.py
```

Model will stored in ./temp


You can use pretrained model in ./models/best.pth.tar for run play test:
```bash
python pit.py
```